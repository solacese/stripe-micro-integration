apiVersion: v1
kind: Service
metadata:
  name: stripe-solace-bridge
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: stripe-solace-bridge
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
