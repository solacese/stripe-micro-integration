apiVersion: v1
kind: ConfigMap
metadata:
  name: stripe-solace-bridge-config
data:
  LOG_LEVEL: "INFO"
  IDEMPOTENCY_TTL_SECONDS: "86400"
  ASYNC_QUEUE_SIZE: "1000"
  MAX_PUBLISH_RETRIES: "3"
  RETRY_BACKOFF_BASE_SECONDS: "0.5"
